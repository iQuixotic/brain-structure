
import React from "react";
import './style.css';

// will map over array and supply form
const Form = (props) => {
    return(
    <div>
        <div id={props.id}  className={props.cn}>

        </div>           
    </div>
    );
}

export default Form;

